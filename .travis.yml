language: python
python:
  - "2.7"
before_install:
  - "wget https://github.com/soulnai/internal/blob/master/tests/unittests/parameters.txt"
  - "ls"
  - "cd tests"
  - "cd unittests"
# command to install dependencies
install:
  - "pip install -r requirements.txt"
# command to run tests
script: "py.test check_bk_return_correct_data.py"